{
  "name": "reshot",
  "description": "reshot monorepo",
  "private": true,
  "scripts": {
    "build:storybook": "pnpm --filter @reshot/storybook-demo build:storybook",
    "dev": "pnpm --parallel -r dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}